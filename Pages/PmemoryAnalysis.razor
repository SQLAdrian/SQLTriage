<!--/* In the name of God, the Merciful, the Compassionate */-->
@page "/pmemory-analysis"
@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo("/memory", replace: true);
    }
}
