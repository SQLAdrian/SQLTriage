<!-- In the name of God, the Merciful, the Compassionate -->

# Panels

Reserved folder for future panel-specific extraction work. Currently empty — all panel content lives in `Components/Shared/DynamicPanel.razor` and its companion files in `Components/Shared/`.

When a panel grows complex enough to warrant its own subtree, extract it here with a one-line README addition.
